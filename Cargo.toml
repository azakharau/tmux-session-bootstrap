[package]
name = "tmux-session-bootstrap"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Create and enter an opinionated three-window tmux session."
repository = "https://github.com/azakharau/tmux-session-bootstrap"

[[bin]]
name = "ts"
path = "src/main.rs"

[dependencies]

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"
